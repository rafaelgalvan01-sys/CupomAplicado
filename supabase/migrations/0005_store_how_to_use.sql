-- Seção extra de conteúdo SEO por loja: "como usar cupom de desconto na
-- {loja}", gerada pelo mesmo script de IA (scripts/generate-seo-content.mjs).
-- Aumenta a profundidade de conteúdo único por página de loja (técnica
-- observada em concorrentes como Cuponomia).

alter table stores add column if not exists how_to_use_content text;
